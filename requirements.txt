# Core
anthropic>=0.40.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
redis>=5.0.0
litellm>=1.0.0

# API
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9

# Temporal Orchestration
temporalio>=1.5.1

# HTTP client (for tool integrations)
httpx>=0.27.0

# Database
sqlalchemy>=2.0.0
alembic>=1.13.0
asyncpg>=0.29.0  # PostgreSQL async driver
aiosqlite>=0.20.0  # SQLite async driver (for dev fallback)

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0

# Dev
black>=24.0.0
ruff>=0.3.0
